spring:
  datasource:
    url: jdbc:postgresql://localhost:6001/spring-2024
    username: postgres
    password: password


  jpa:
      show-sql: true
      hibernate:
        ddl-auto: update
      database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
      init:
        mode: always